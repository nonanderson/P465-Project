<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add Listing</title>
</head>
<body>
     <div class="register-container">
        <form action=" /add-listing" method="POST">
            <h1>Create a listing</h1>
            <div class="form-group">
                
                <input type="userEmail" id="userEmail" name="userEmail" class="form-control"
                    placeholder="Enter Email" {{#if userEmail}} value={{userEmail}} {{/if}} />
                    <label for="userEmail">Email</label>
            </div>
            <div class="form-group">
                
                <input type="streetAddress" id="streetAddress" name="streetAddress" class="form-control"
                    placeholder="Enter Street Address" {{#if streetAddress}} value={{streetAddress}} {{/if}} />
                    <label for="streetAddress">Street Address</label>
            </div>
            <div class="form-group">

                <input type="city" id="city" name="city" class="form-control" placeholder="Enter city"
                    {{#if city}} value={{city}} {{/if}} />
                    <label for="city">City</label>
            </div>
            <div class="form-group">
                {{!--Turn into dropdown menu--}}
                <input type="state" id="state" name="state" class="form-control" placeholder="Enter State" {{#if email}}
                    value={{email}} {{/if}} />
                    <label for="state">State</label>
            </div>
            <button type="submit" class="register-button">
                Submit
            </button>
        </form>
    </div>
</body>
</html>